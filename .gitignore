node_modules/

# Published deployment templates. Keep these tracked even if broader local
# ignore rules are added later; runtime data and secrets remain ignored below.
!Dockerfile
!.dockerignore
!config.container.json
!compose.dokploy.yaml

# Normalized app-owned runtime storage and local SQLite artifacts
/data/
/logs/
*.db
*.db-journal
*.db-wal
*.db-shm

# Local configuration
.config
.config.example

# Legacy migration-compatibility root files (not normalized runtime outputs)
/.server.log
/.server.err.log
/.callback-*.log
/.callback-*.err.log

# Legacy/workspace-local repo-root scratch; normalized runtime temp files live under /data/tmp/
/tmp/

# Workspace-local files outside app self-cleanup scope
record.json
Typora_Hook_Log.txt
.agents/
.claude/
.superpowers/

# Development-only files kept locally but not published
.editorconfig
SKILL.md
TODOS.md
docs/
!docs/
docs/*
!docs/agent-audit-log-integration-guide.md
!docs/agent-automation-efficiency.md
!docs/dokploy-deployment.md
!docs/feishu-bot-notification-design.md
!docs/superpowers/
!docs/superpowers/specs/
!docs/superpowers/specs/2026-07-20-audit-demo-log-sender-design.md
v1.5/

# Local-only demo verification; keep the runnable production script tracked.
/test/send-audit-demo-logs.test.js

# OS/editor noise
.DS_Store
